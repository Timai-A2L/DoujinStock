# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DoujinStock' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Realm'
  pod 'RealmSwift'

  # Pods for DoujinStock

  target 'DoujinStockTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DoujinStockUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
